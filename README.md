# Install
```
choco install -y neovim git mingw make ripgrep wget fd unzip gzip
```

Powershell:
```ps1
git clone https://github.com/grollgor/nvim ${env:LOCALAPPDATA}
```

## C#
```ps1
dotnet tool install -g dotnet-format
```
